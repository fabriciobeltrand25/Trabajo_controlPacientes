<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medicos"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="29"
        android:targetSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher2"
        android:label="Control Médicos"
        android:theme="@android:style/Theme.Holo.Light" >

        <!-- Login debe ser la principal (LAUNCHER) -->
        <activity
            android:name=".LoginActivity"
            android:label="Login"
            android:windowSoftInputMode="adjustResize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (Menú) -->
        <activity 
            android:name=".MainActivity" 
            android:windowSoftInputMode="adjustResize" />

        <!-- Pacientes -->
        <activity 
            android:name=".PacientesActivity" 
            android:windowSoftInputMode="adjustResize" />

        <!-- Médicos -->
        <activity 
            android:name=".MedicosActivity" 
            android:windowSoftInputMode="adjustResize" />

        <!-- Consultas -->
        <activity 
            android:name=".ConsultasActivity" 
            android:windowSoftInputMode="adjustResize" />

        <!-- Cobros -->
        <activity 
            android:name=".CobrosActivity" 
            android:windowSoftInputMode="adjustResize" />

        <!-- Reportes -->
        <activity 
            android:name=".ReportesActivity" 
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>